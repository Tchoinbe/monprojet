	
        #include<stdio.h>
        #include<stdlib.h>
	#include<omp.h>

	int main(arg,char const,*argv)

	{

     in N;

	float A[N], B[n], C[N];

	for (int = 0 : i < N : i++)

	c[i]= B[i] + A[i];

	}
